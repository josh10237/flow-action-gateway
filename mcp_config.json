{
  "servers": [
    {
      "name": "filesystem",
      "display_name": "Files",
      "icon": "📁",
      "enabled": "${MCP_FILESYSTEM_ENABLED}",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "${MCP_FILESYSTEM_PATH}"
      ],
      "env": {}
    },
    {
      "name": "github",
      "display_name": "GitHub",
      "icon": "🐙",
      "enabled": "${MCP_GITHUB_ENABLED}",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-github"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_PERSONAL_ACCESS_TOKEN}"
      }
    }
  ]
}
